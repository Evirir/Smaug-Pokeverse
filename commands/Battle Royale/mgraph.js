const Discord = require('discord.js');

module.exports = {
    name: 'mgraph',
    description: 'Does nothing yet',
    wip: true,

    async execute(message, args, prefix) {
        message.channel.send(`Nothing here u.=.u`);
    }
};
