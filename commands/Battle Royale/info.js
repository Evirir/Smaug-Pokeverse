const Discord = require('discord.js');
const GameGraph = require('../../models/gameGraph.js');

module.exports = {
    name: 'info',
    description: 'Shows the current game info.',

    async execute(message, args, prefix) {

    }
};
